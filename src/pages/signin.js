export const getServerSideProps = async (context) => {
  return {
    props: {},
  };
};

const SignIn = (props) => {
  return (
    <div id="firebaseui-auth-container">
      <p>HEY</p>
    </div>
  );
};

export default SignIn;
